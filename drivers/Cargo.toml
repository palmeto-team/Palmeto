[package]
name = "drivers"
version.workspace = true
edition.workspace = true

[dependencies]
shared = { path = "../shared" }
pine   = { path = "../pine" }

spin   = { workspace = true }
fdt    = { workspace = true }
